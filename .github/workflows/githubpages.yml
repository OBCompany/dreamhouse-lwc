name: CI

on: workflow_dispatch

jobs:

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
       # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v5

      - name: Install Salesforce CLI 
        run: |
                mkdir -p ~/cli/sf
                wget -qO- https://developer.salesforce.com/media/salesforce-cli/sf/channels/stable/sf-linux-x64.tar.xz | tar xJ -C ~/cli/sf --strip-components 1
                export PATH=~/cli/sf/bin:$PATH
                echo "$PATH" >> $GITHUB_PATH
                echo "path is : ${PATH}"
                echo "github path is : ${GITHUB_PATH}"
                sf --version
                

      - name:  Run PMD Scanner
        run: |
           sf code-analyzer run --target force-app --output-file --help
           sf code-analyzer run --target force-app --rule-selector all --output-file results.html
           ls -al

      - name: Setup Pages
        if: github.ref == 'refs/heads/main'
        uses: actions/configure-pages@v5

      - name: Upload PMD Scanner reports
        uses: actions/upload-artifact@v5
        with:
          name: pmd-report
          path: ./results.html

      - name: Upload Artifact
        if: github.ref == 'refs/heads/main'
        uses: actions/upload-pages-artifact@v3
        with:
          # location of the coverage artifacts
          path: ./results.html

  deploy-coverage:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    needs: test
    
    permissions:
      pages: write
      id-token: write

    environment:
      # environment created automatically by GitHub
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
